name: CI

on:
  pull_request:
  push:
    tags:
      - 'v*'
    branches:
      - 'main'
      - 'feature/*'

jobs:
  pre-commit:
    name: "Run Pre-Commit Checks"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-python@v5
      - uses: pre-commit/action@v3.0.1
  build:
    name: "Build Artifacts"
    needs: pre-commit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-python@v5
      - uses: pre-commit/action@v3.0.1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Compute version
        id: version
        run: |
          # If this run is for a tag (refs/tags/...), compute a cleaned version;
          # otherwise set a safe default version for non-tag pushes.
          if [[ "${GITHUB_REF:-}" =~ ^refs/tags/ ]]; then
            echo "VERSION=$(git describe --tags || git describe --always || echo "v0.0.0")" >> $GITHUB_OUTPUT
          else
            echo "VERSION=0.0.0" >> $GITHUB_OUTPUT
          fi
      - name: Build
        uses: docker/build-push-action@v6
        with:
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=local,dest=./artifacts
          build-args: |
            APP_VERSION=${{ steps.version.outputs.VERSION }}
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openmower-cli
          path: artifacts/
  tagged-release:
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag' && startsWith(github.ref_name, 'v')
    needs: build
    permissions:
      contents: write
    steps:
      - name: Download artifacts from build stage
        uses: actions/download-artifact@v4
        with:
          name: openmower-cli
          path: artifacts
      - name: Compress release directory into a versioned ZIP file
        run: cd artifacts && zip -r "../openmower-cli-${{ github.ref_name }}.zip" .
      - uses: softprops/action-gh-release@v2
        with:
          prerelease: false
          files: "openmower-cli-${{ github.ref_name }}.zip"
